const divide = (num1, num2) => {
    console.log (`Dividing ${num1}`);
    console.log (`Dividing ${num2}`);
    return num1 / num2;
}

const square = (num3) => {
    console.log (`Multiplying ${num3}`)
    return num3 * num3;
}

const add = (num4, num5) => {
    console.log (`Adding ${num4}`)
    console.log (`Adding ${num5}`)
    return num4 + num5;
}